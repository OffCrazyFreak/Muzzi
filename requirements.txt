# Pinned to the versions this library was built and verified with, on Python
# 3.12. Install into a local venv:
#
#   python3 -m venv .venv && ./.venv/bin/pip install -r requirements.txt
#
# ffmpeg, yt-dlp, Node/Deno and bin/fpcalc are not pip packages. See README.md.

# ------------------------------------------------------------------ pipeline
beets==2.13.1
pyacoustid==1.3.1
essentia==2.1b6.dev1389
faster-whisper==1.2.1
mutagen==1.48.1
numpy==2.4.6
langdetect==1.0.9
requests==2.34.2
PyYAML==6.0.3
odfpy==1.4.1
pillow==12.3.0
ytmusicapi==1.12.2

# ------------------------------- required by the beets plugins in config.yaml
# discogs, lastgenre, lyrics and the Discogs OAuth flow. beets does not declare
# these itself, so leaving one out fails at plugin load, not at install.
discogs-client==2.3.0
pylast==7.1.0
beautifulsoup4==4.15.0
requests-oauthlib==2.0.0
requests-ratelimiter==0.10.0

# ----------------------------------------------- not needed by every pipeline
# Installed by the line above, deliberately: this file is the whole install,
# and both are small. Neither is imported at module level, so the pipeline
# still runs if you strip them out of your own copy.
#
# vibenet: mood and danceability. analyze.py loads it in a try/except and
# degrades silently without it.
vibenet==0.1.0
# pycryptodome: tools/export_cookies.py only, to decrypt Chromium cookies.
pycryptodome==3.23.0
