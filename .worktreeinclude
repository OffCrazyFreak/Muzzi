# Gitignored files copied into every new worktree, .gitignore syntax.
# Only files that match AND are gitignored are copied, so tracked files
# are never duplicated. This lists the names of secrets, not the secrets.
#
# hints.tsv is deliberately absent: a copy per worktree would collect
# separate review answers and the copies would diverge with no way to merge.
config/secrets.json
config/config.yaml
